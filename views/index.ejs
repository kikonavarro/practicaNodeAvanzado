<!DOCTYPE html>
<html>
	<head>
		<title><%= title %></title>
		<link rel="stylesheet" href="/stylesheets/style.css" />
	</head>
	<body>
		<h1>Bienvenidos a <%=title %></h1>

		<h2>Estos son los artículos de compra y venta que tenemos hoy:</h2>

		<% anuncios.forEach(anuncio => { %>
		<h1><%= anuncio.nombre %></h1>
		<% if (anuncio.venta == true) {%>
		<p>Artículo en venta</p>
		<% } else { %>
		<p>Busco para comprar</p>
		<% } %>

		<p><%= anuncio.precio %> euros</p>
		<p>#<%= anuncio.tags %></p>
		<img
			src="../images/<%= anuncio.foto %>"
			alt="imagen producto"
			style="max-width: 300px; max-height: 300px"
		/>

		<% }); %>
	</body>
</html>
